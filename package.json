{
  "name": "aponar-nihon",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "Static-first Japanese learning platform with TypeScript frontend, Python content tooling, Node.js quality automation and Cloudflare Worker APIs.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build:ts": "npx --yes --package typescript@5.9.2 tsc -p tsconfig.web.json",
    "build:site": "python3 tools/build_site.py --out _site --check-links",
    "build": "npm run build:ts && npm run build:site",
    "quality": "node tools/web_quality.mjs _site",
    "verify": "npm run build && npm run quality",
    "test:browser": "playwright test",
    "lighthouse": "lhci autorun",
    "worker:dev": "npx --yes wrangler@4 dev",
    "worker:deploy": "npx --yes wrangler@4 deploy"
  }
}
